import React, { useState } from 'react';

interface IRoutes {
  route: string;
}

function Routes() {
const [ routes, setRoutes ] = useState<IRoutes[]>([]);

  return (
    <div></div>
  );
}

export default Routes;
